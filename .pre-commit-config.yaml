repos:
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v18.1.3  # Must match CI (.github/workflows/clang-format-check.yml)
    hooks:
      - id: clang-format
        files: \.(cpp|hpp|c|h)$
        exclude: ^(build|build-.*)/
        stages: [pre-commit, pre-push]
